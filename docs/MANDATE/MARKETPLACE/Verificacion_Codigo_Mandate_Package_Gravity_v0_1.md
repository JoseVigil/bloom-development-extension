Restricción de alcance heredada del encargo madre — no negociable: este documento es resultado de investigación de solo lectura. Ningún hallazgo de código habilita por sí solo un cambio de código; no se modificó ni se propone modificar ningún archivo bajo `installer/`, `src/`, `backend/`, `BloomNucleus/`, `brain/`, `webview/` ni ningún otro directorio de código de este repositorio.

# Verificación de Código — BLOOM Mandate Package × Gravity × Wisdom v0.1

**Tipo:** Reporte de verificación código-vs-documentación — cierra §6 de `BLOOM_Mandate_Package_Gravity_Wisdom_Integracion_Spec_v0_1.md` (el encargo madre, en adelante **Encargo**)
**Estado:** Investigación completa — NO decisión tomada. Entrega los insumos que el Encargo pide en su §10 para que José decida.
**Fecha:** 2026-09-09
**Repositorio verificado:** `/home/jose/repos/bloom-development-extension` (lectura vía shell local, sin clonar)
**Método:** cada afirmación de este documento fue confirmada leyendo el archivo fuente citado (código o `.md`) o ejecutando `git log`/`grep` contra el repo real — nunca por memoria ni inferencia. Se sigue la misma disciplina de cita que el Encargo usa en su §1 (D1–D6): archivo + línea/función, o hash de commit + fecha cuando la evidencia es histórica.

---

## Resumen ejecutivo

Las preguntas C1–C6 del Encargo tienen ahora respuesta directa contra el repositorio real. La conclusión más importante no está en ninguna fila individual de la tabla, sino en la combinación de C1 y C2: **`governance` no existe embebido en `mandate.json` en ningún punto del código real, y `nucleus mandate publish`/`install` no existen en absoluto** — ni como comando, ni como concepto, ni como string en ningún archivo Go/TS/Python del repositorio. Esto no es "falta implementar sobre una base existente" (la lectura optimista que el Encargo dejaba abierta en su §8 punto 3): es que el modelo real de Gravity es arquitectónicamente distinto del que asume toda la Sección 3–5 del Encargo. Gravity vive en un árbol de nodos separado (`GravityNode`, con su propio `Store` y su propio directorio `.gravity/`), vinculado a un Mandate por `mandateId`/`sourceMandateId` — nunca embebido dentro de `mandate.json` como un bloque `governance`. Este punto se desarrolla en la fila C1 y se retoma en la Sección 3 de este documento.

Por el lado positivo: Gravity en sí (posturas, nodos, resolución por turno, cálculo de Masa) **sí** está sustancialmente implementado y en producción de diseño — no es solo pseudocódigo de spec. El campo `posture.status` que el Encargo (D4) señalaba como posiblemente adelantado a su propio schema documental **sí existe en código real** y se usa activamente en la resolución (C3). El path `.bloom/.nucleus-{organization}/.gravity/` que el Encargo (D5) solo podía confirmar contra un diagrama de BTIPS **también se confirma en código** (C4). Lo que no existe en absoluto es la capa de Marketplace/Package: ni publish/install, ni `compliance.linter`, ni Genes portables, ni el concepto de "Mandate Package" — cero menciones en todo el código fuente.

---

## 1. Tabla C1–C6 — respuesta contra el repositorio real

| # | Pregunta | Respuesta | Evidencia |
|---|---|---|---|
| **C1** | ¿`mandate.json` real tiene un bloque `governance` implementado? | **No.** No existe en ningún punto del código un campo, struct, ni tag JSON `governance`. El schema real de `mandate.json` (`MandateJSON`, la forma que efectivamente escribe la activity de firma) no lo tiene, y tampoco lo tiene el documento que esa misma activity cita como fuente del shape completo. Gravity se modela como un **árbol de nodos separado**, no embebido. | `grep -rn 'json:"governance"' installer/` → 0 resultados. Struct real: `installer/nucleus/internal/orchestration/activities/mandate_genesis_sign_activity.go:149-156` (`type MandateJSON struct { MandateID, MandateType, Project, Status, SignedAt, Operational }` — sin `Governance`), con comentario explícito en línea 147-148: *"MandateJSON es la forma mínima... No es el shape completo de un standard firmado (eso es Command Surface v0.2.0)"*. Se verificó esa referencia: `docs/MANDATE/BLOOM_Mandate_CLI_Command_Surface_v0_2_0.md` (368 líneas) — `grep -ci "governance\|gravity"` → 0. Coincide con `docs/MANDATE/Mandate_Domain_Spec_v1.0.0.md` §4.1 (líneas 207–289), que ya confirmaba esto documentalmente (D3 del Encargo). El árbol separado de Gravity está en `installer/nucleus/internal/gravity/model.go:64-89` (`GravityNode` con `NodeType: MANDATE`, vinculado por `nodeId`), creado y enlazado desde `installer/nucleus/internal/orchestration/activities/mandate_gravity_session_activities.go` (`EnsureGravityMandateNodeActivity`, líneas 145-154). |
| **C2** | ¿Existe `nucleus mandate publish`/`install`? | **No, en absoluto.** Ni el comando, ni el concepto "Mandate Package"/"Marketplace" aparecen en ningún archivo del repositorio. | `installer/nucleus/internal/orchestration/commands/mandate.go` define exactamente 4 sub-comandos bajo `mandate`: `create` (L65), `build` (L271), `status` (L464), y el comando raíz (L24) — no hay `publish` ni `install`. `grep -rliE "mandate.?package\|marketplace" --include="*.go" installer/nucleus/` → 0 resultados. También 0 resultados para `compliance.?linter`, `geneBlueprintCount`, `gene_blueprint` en `installer/`, `src/`, `backend/`, `BloomNucleus/`, `brain/` (Go+TS+Python). |
| **C3** | ¿Existe `posture.status` en la implementación real? | **Sí, existe y se usa activamente**, a diferencia de lo que el Encargo (D4) dejaba como posible caso de pseudocódigo adelantado a su propio schema. Existe tanto a nivel de postura como a nivel de nodo — dos campos `status` distintos, ambos reales. | `installer/nucleus/internal/gravity/model.go:70` — `GravityPosture.Status string \`json:"status"\`` (nivel postura) y línea 83 — `GravityNode.Status NodeStatus` (nivel nodo, con enum `NodeActive/NodeSuperseded` en L17-22). Uso real en la resolución por turno: `installer/nucleus/internal/gravity/resolver.go:52` (`if node.Status != NodeActive`) y **línea 56** (`if posture.Status != "active" || !applies(...)`) — reproduce exactamente el pseudocódigo de `Orbital_Gravity_Implementation_Spec_v0_1.md:115`. `gravity/model.go` fue agregado en el commit `52ff878` (2026-08-29 09:38:06 -0300). Nota documental: `BLOOM_Mandate_Universal_Schema_v1_2_0.md` (147 líneas, leído completo) **nunca define** un campo `status` a nivel de postura individual — ver Sección 2, discrepancia adicional C más abajo. |
| **C4** | ¿Existe `.bloom/.nucleus-{organization}/.gravity/` y qué formato usa? | **Sí, confirmado en código**, combinando dos módulos independientes. Formato: un archivo JSON por nodo (`GravityNode`), con subdirectorios `.edges/` e `.index/`. | `installer/nucleus/internal/core/org_context.go:155` — `filepath.Join(homeDir, ".bloom", fmt.Sprintf(".nucleus-%s", orgSlug))` (raíz de organización). `installer/nucleus/internal/gravity/store.go:29` — `NewStore(nucleusRoot)` construye `filepath.Join(nucleusRoot, ".gravity")` como raíz del store. Combinados: `.bloom/.nucleus-{org}/.gravity/`, exactamente como afirma D5 del Encargo (que solo podía citar el diagrama de BTIPS). Layout físico: `store.go:36-43` (`EnsureLayout`) crea `.gravity/`, `.gravity/.edges/`, `.gravity/.index/`. Formato JSON confirmado en `store.go:45-61` (`ReadNode` hace `json.Unmarshal` a `GravityNode`) y `store.go:63-90` (`CreateNode`, un archivo por nodo, `nodeVersion` inicial = 1). Comentario adicional en `mandate_gravity_session_activities.go:24-32` confirma la convención de paths jerárquicos (`.organization/{id}/.project/{id}/.mandate/{id}/.session/{id}/`) como espejo deliberado de `resolver.go` (`readSpine`), no una reinvención. |
| **C5** | ¿Existe (de)serialización para `gravityPostures[]`/`inheritedGravityPostures[]`? | **Sí para `gravityPostures[]`** (marshal/unmarshal estándar de Go vía tags JSON en `GravityPosture`, usado por `store.go`). **No existe `inheritedGravityPostures[]` como estructura separada** — el código no modela dos arreglos distintos ("propias" vs "heredadas") como sí lo hace `v1.2.0` del schema documental. La herencia se modela con un único arreglo `GravityPostures[]` por nodo, distinguido por el campo `Origin` (enum `mandate_own` / `mandate_inherited`, entre otros). | Struct: `installer/nucleus/internal/gravity/model.go:64-76`. Enum de origen: L24-33 (`PostureOrigin`, valores `nucleus`, `organization`, `project`, `mandate_own`, `mandate_inherited`, `session`). `grep -rn "InheritedGravityPostures\|inheritedGravityPostures" installer/` → 0 resultados. Esto es una diferencia de forma (un arreglo con campo discriminador vs. dos arreglos separados), no solo de nombre — afecta directamente el punto [PENDIENTE-JOSÉ] del Encargo §3.2. |
| **C6** | ¿Es extensible el `compliance.linter` real? | **La pregunta no aplica — el linter no existe.** No hay ninguna implementación de `compliance.linter` en ningún lenguaje del repo, ni referencias a las invariantes I-1 a I-12 en código. | `grep -rliE "compliance\.linter\|complianceLinter"` sobre `installer/`, `src/`, `backend/`, `BloomNucleus/`, `brain/` (Go+TS+Python) → 0 resultados. `grep -rn "I-7\|I-8\b\|I-9\b\|I-12\b" --include="*.go" installer/` → 0 resultados. Consecuencia directa para el Encargo §6.1 (fila C6, "costo real de implementar I-13"): no es un costo de refactor de un linter existente — es construir el linter completo desde cero, junto con las 6 invariantes I-7 a I-12 que hoy tampoco tienen ninguna validación en código (siguen siendo, en la práctica, solo texto normativo en `BLOOM_Mandate_Package_Spec_v1_0_0.md`). |

---

## 2. Discrepancias documentación-vs-código adicionales a §1 del Encargo (D1–D6)

Estas se suman al diagnóstico del Encargo, no lo reemplazan — siguiendo la misma regla de continuidad que el Encargo fija en su cierre.

**Discrepancia A — las versiones intermedias del Universal Schema que v1.2.0 dice extender no existen como archivo, y una de ellas fue borrada en el mismo commit que introdujo v1.2.0.**
`docs/MANDATE/BLOOM_Mandate_Universal_Schema_v1_0_0.md` existió como archivo físico (commit `d8f4a83`, 2026-06-30 21:16:12, *"docs(nucleus): add Mandate Universal Schema v1.0.0"*) y fue **borrado** en el commit `0fb2564` (2026-08-27 21:47:20), el mismo commit que agregó lo que hoy es `docs/MANDATE/BLOOM_Mandate_Universal_Schema_v1_2_0.md` (verificado con `git log --diff-filter=D` sobre el path v1_0_0, y `git log --diff-filter=A` sobre el path v1_2_0 — ambos apuntan al mismo hash). `v1_0_1.md` y `v1_1_0.md` **nunca existieron** como archivos físicos en todo el historial de git (`git log --all --diff-filter=A --name-only -- "*Mandate_Universal_Schema*"` solo encuentra los paths `v1_0_0.md` y `v1_2_0.md`, nada más). Sin embargo, el archivo vigente hoy (`v1_2_0.md`) sigue citando en su cabecera *"Extiende a: `v1.0.0.md`, `v1.0.1.md`, `v1.1.0.md` (todos permanecen vigentes sin cambios)"* (línea 9) y remite repetidamente a secciones puntuales de "v1.1.0" (§2.1, §3.1, §3.2, §4, §5, §7 — líneas 46, 61, 68, 74, 84, 99, 101, 109) que no son verificables en ningún archivo del repo. El mismo patrón se repite en `docs/ORBITAL/GRAVITY/Orbital_Gravity_Implementation_Spec_v0_1.md:12` (*"Depende de: `BLOOM_Mandate_Universal_Schema_v1_0_0/v1_0_1/v1_1_0/v1_2_0.md`"*) y en `docs/ANALYSIS/GRAVITY/GRAFO/Orbital_Gravity_Persistencia_Grafo_Implementation_Spec_v0_1.md` (cabecera, *"Depende de, sin reabrir: `BLOOM_Mandate_Universal_Schema_v1_0_0`–`v1_2_0.md`"*). Esto profundiza D2 del Encargo: no es solo que el Package Spec dependa de una versión anterior del schema — es que dos de las tres versiones intermedias que el propio schema vigente dice extender no son un documento verificable en absoluto, en ningún punto de la historia del repositorio. El schema real de una `gravityPosture` individual (qué campos tiene, cuáles son opcionales) no tiene, hoy, ningún documento fuente autosuficiente — se reconstruye por inferencia a partir de ejemplos en `v1.2.0` §5 y de otros tres documentos derivados (ver Discrepancia C).

**Discrepancia B — el campo `posture.status` existe en código pero no está definido en el Universal Schema vigente, y un documento derivado se contradice a sí mismo sobre dónde vive.**
Ya establecido en C3 que `GravityPosture.Status` existe en código real y se usa en `resolver.go:56`. `BLOOM_Mandate_Universal_Schema_v1_2_0.md` (147 líneas, leído íntegro) no define este campo en ningún punto — ni en el bloque `governance.inheritedGravityPostures[]`/`gravityPostures[]` de su §2, ni en el ejemplo de su §5. Tres documentos derivados sí lo mencionan, con relatos distintos entre sí:
- `docs/ORBITAL/GRAVITY/Orbital_Gravity_Implementation_Spec_v0_1.md:56-65` (§1.2) lista `status` como campo **raíz** de `GravityNode` (junto a `nodeId`, `nodeType`, `parentId`), no de cada postura — y su propio pseudocódigo de la línea 115 (`for posture in node.gravityPostures where posture.status == "active"`) lo usa como si fuera de la postura, contradiciendo su propio §1.2. Esta es exactamente la D4 que el Encargo ya señala.
- `docs/ANALYSIS/GRAVITY/GRAFO/Orbital_Gravity_Persistencia_Grafo_Implementation_Spec_v0_1.md`, en su tabla de "extensiones consolidadas" (§7, línea 394), reafirma que `status` es un campo **ya existente y sin cambios** en la raíz de `GravityNode` (agrupado junto a `nodeId`, `nodeType`, `parentId`, `createdAt`) — pero el ejemplo JSON de esa misma sección, dos párrafos antes (§5.3, líneas ~296-309), muestra `"status": "active"` **anidado dentro de un elemento de `gravityPostures[]`**, sin declararlo como campo nuevo en la tabla de §7 que se supone lo resume todo.
- `docs/ANALYSIS/GRAVITY/GRAMMAR/Orbital_Gravity_Expression_Grammar_Parser_Spec_v0_1.md:42` sí lista `status` explícitamente como parte del schema del objeto `gravityPostures[]` (junto a `postureId`, `appliesTo[]`, `authoredBy`, `verifiable`, `promotable`, `promotedTo`, `promotedFrom`), citando como fuente "Persistencia §5.3" — la misma sección que se contradice con la tabla §7 del propio documento que cita.
- `docs/ANALYSIS/BACKEND/GRAVITY/Investigacion_Gravity_Mandate_Compatibility_v0_1.md:17` afirma que `status` es parte del schema de cada elemento de `gravityPostures[]`, citando "Mandate Universal Schema v1.2.0 §2/§5" — cita que no se sostiene: esas secciones de `v1.2.0`, leídas directamente, no incluyen `status` (ver arriba).

El código real (`model.go`) resuelve la ambigüedad de facto — `status` existe en **ambos** niveles, postura y nodo, como dos campos independientes (L70 y L83) — pero ningún documento describe esto correctamente ni de forma consistente entre sí. Es una discrepancia documento-contra-documento, no solo documento-contra-código.

**Discrepancia C — `docs/MANDATE/Mandate_Domain_Spec_v1.0.0.md` §6.3 documenta comandos CLI (`run`, `pause`, `resume`) que no existen en `mandate.go`.**
El código real (`installer/nucleus/internal/orchestration/commands/mandate.go`) solo implementa `create`, `build`, `status`. No es una regresión: el documento se autodenomina en su cabecera (línea 3) *"Febrero 2026 · Entorno Simulado"* y en su tabla de estado (línea 9) *"Estado: Especificación — Pendiente Implementación"* — es decir, ya declara no ser una descripción del código real. Se registra igual porque este mismo documento es la fuente citada por D3 del Encargo para el schema de `mandate.json`, y conviene que quien lo use sepa que su sección de comandos (§6.3) no debe leerse como estado actual.

**Discrepancia D — colisión de nombre "Governance CLI".**
El título de la §6.3 recién mencionada es literalmente "Governance CLI" — pero se refiere al grupo de verbos de ciclo de vida de un Mandate (`create`/`run`/`pause`/`resume`/`status`), sin ninguna relación con el bloque `governance` de Gravity que aparece 6 meses después en `v1.2.0`. Coincidencia de nombre, no de concepto — vale una nota para quien busque "governance" en el repo y encuentre ambos usos sin relación entre sí.

**Discrepancia E — copias obsoletas de los documentos de Wisdom bajo `_to_delete/`.**
`docs/MANDATE/MARKETPLACE/_to_delete/BLOOM_Wisdom_Sintesis_Codex_v0_1.md` y `.../BLOOM_Wisdom_Handshake_Investigacion_v0_1.md` son copias que debían eliminarse en el commit `f94536a` (2026-08-29 11:56:21, *"Move Wisdom reference docs into their own docs/WISDOM directory"*) pero quedaron sin borrar en la ubicación vieja, junto a las copias vigentes en `docs/WISDOM/`. No es una discrepancia de contenido (son las mismas versiones, solo duplicadas), pero es un riesgo de lectura — cualquier búsqueda que no filtre `_to_delete/` puede citar la ubicación equivocada. Fuera del alcance de este cowork moverlas o borrarlas (son código de repositorio, no un cambio de contenido documental), se deja señalado para limpieza manual.

---

## 3. El hallazgo que reabre la premisa de las Secciones 3–5 del Encargo

Este punto no estaba en la lista C1–C6 pero es la consecuencia más directa de responderlas: toda la Sección 3 del Encargo (invariante I-13, tokenización de `sourceMandateId`, extensión de `manifest.json`, extensión de `compliance.linter`) y las Secciones 4–5 (flujos de `publish`/`install`) están escritas asumiendo que `gravityPostures[]`/`inheritedGravityPostures[]` son campos **dentro de `mandate.json`**, exactamente como los define `v1.2.0` del Universal Schema. Eso es correcto como descripción del *documento* — pero el *código real* no tiene ningún bloque `governance` en `mandate.json`, y modela Gravity como un árbol de nodos completamente separado (`GravityNode`, con su propio store, su propio directorio, su propio ciclo de vida), enlazado a un Mandate únicamente por `mandateId`.

Esto no invalida el trabajo de diseño del Encargo — el problema que identifica (Gravity no viaja cuando un Mandate se exporta) sigue siendo real y sin resolver. Pero cambia la pregunta de diseño de fondo: **¿qué exporta un Mandate Package cuando "empaqueta Gravity" — el contenido de un bloque `governance` que hoy no existe en ningún `mandate.json` real, o el nodo `GravityNode` de tipo `MANDATE` (y su cadena de herencia `mandate_inherited`) que sí existe, vive en un archivo separado bajo `.gravity/`, y es lo que el resolver de turno (`resolver.go`) realmente consulta?** Ninguna de las dos respuestas está mal — son dos arquitecturas distintas y el Encargo diseñó sobre la primera sin poder saber, solo con los `.md`, que la segunda es la que existe en código. Se dejó como punto nuevo en la Sección 4 de este documento (§8, punto 5) para que José decida antes de que cualquier trabajo futuro continúe sobre §3–§5 del Encargo tal como están escritas hoy.

---

## 4. Insumos para los bloqueadores de gobernanza — §8 del Encargo

No se resuelve ninguno de estos por inferencia; se entrega lo que la investigación de código aporta a cada uno, para que la decisión siga siendo de José.

1. **Tratamiento de linaje truncado (§3.2 del Encargo).** Sin novedad desde el código: no existe en el repositorio ningún concepto de exportación/tokenización de `sourceMandateId`, porque no existe `inheritedGravityPostures[]` como estructura separada (C5) ni el flujo de `publish`/`install` en absoluto (C2). Sigue siendo una decisión enteramente abierta, sin precedente de código sobre el cual apoyarse.
2. **`status` de postura (D4/C3).** Con novedad: **ya existe en código real, en ambos niveles** (postura y nodo — Sección 1, fila C3). No hace falta decidir *si* se formaliza — ya está implementado y en uso activo en `resolver.go`. Lo que sí sigue abierto es *documentarlo correctamente*: el campo no tiene tipo enum restringido en código (`Status string`, sin constantes como las que sí tiene `NodeStatus` para el nodo) y ningún documento fuente lo define de forma consistente (Discrepancia B). Sugerencia para decisión de José: si vale la pena, en un futuro `v1.3.0` del Universal Schema, (a) declarar `status` como campo formal de cada elemento de `gravityPostures[]`, y (b) fijar sus valores válidos — hoy el único valor observado en código y ejemplos es `"active"`, sin contraparte documentada (¿existe `"superseded"`/`"deprecated"` a nivel de postura, o ese vocabulario es exclusivo del nodo?).
3. **Orden de trabajo real (C1/C2).** Con novedad, y más profunda que "implementar antes de empaquetar": no es que falte construir `governance` sobre una base existente — es que **la base existente (el árbol `GravityNode`) es una arquitectura distinta** de la que el Encargo asume. Ver Sección 3 de este documento. La decisión que esto habilita no es solo de secuencia (qué se construye primero) sino de **diseño**: si el Mandate Package debe transportar un bloque `governance` embebido (implementando desde cero lo que hoy no existe en `mandate.json`) o si debe transportar una proyección del `GravityNode` real ya existente (tipo `MANDATE`) junto al Mandate, cambiando en consecuencia buena parte de §3–§5 del Encargo.
4. **Alcance de I-13.** Sin novedad de código — no hay ningún `compliance.linter` ni ninguna invariante I-7 a I-12 implementada (C6) sobre la cual comparar el comportamiento propuesto. Sigue siendo decisión enteramente de José, sin materia de código para apoyarla.
5. **[NUEVO] Modelo de exportación: `governance` embebido vs. proyección de `GravityNode`.** Ver Sección 3. Es, en la práctica, prerrequisito de los puntos 1 y 3 — no puede resolverse el tratamiento de `sourceMandateId` tokenizado ni secuenciarse la implementación sin decidir primero qué estructura real se está empaquetando.

---

## 5. Entregables del Encargo §10 — estado de cumplimiento

1. Tabla C1–C6 completa con cita exacta de código: **entregada, Sección 1.**
2. Tabla de discrepancias documentación-vs-código adicionales a §1: **entregada, Sección 2 (A–E).**
3. Decisión de José sobre los cuatro puntos de §8: **no corresponde a este cowork — permanecen abiertos, con insumos nuevos en la Sección 4 (incluye un quinto punto nuevo).**
4. Mapeo campo-por-campo si `governance` existiera en el Mandate real: **no aplica — C1 confirma que no existe.**
5. Propuesta de secuencia de implementación, dado que C1/C2 confirman que `governance` no existe y `publish`/`install` tampoco: **no se redacta como propuesta cerrada en este documento** — la Sección 3 explica por qué cualquier secuencia (Mandate real primero vs. empaquetado después) depende de resolver primero el punto 5 nuevo de §8 (qué se empaqueta realmente). Redactar una secuencia de implementación sin esa decisión previa repetiría el mismo problema que este documento acaba de señalar en el Encargo original.

---

## 6. Nota de alcance

Ningún archivo de código fue modificado, refactorizado, ni tocado de ninguna forma para producir este documento. Todo el contenido de las Secciones 1–5 proviene de lectura (`cat`, `grep`, `git log`) contra el repositorio en `/home/jose/repos/bloom-development-extension`, ejecutada desde una sesión con acceso de solo lectura de facto (no se realizó ningún `git commit`, `git add`, ni edición de archivo bajo ningún directorio de código). Este documento en sí es el único artefacto producido por este cowork, y es documentación — no código.

---

*Fin del reporte de verificación v0.1. Responde §6 (C1–C6) y §10 del Encargo `BLOOM_Mandate_Package_Gravity_Wisdom_Integracion_Spec_v0_1.md`. No decide ninguno de los puntos de §8 — los deja listados en la Sección 4 con los insumos nuevos que este cowork pudo aportar desde el código real.*
